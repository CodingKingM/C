#include <stdio.h>

int main ()
{

    int x; // variable declaration
    x = 10; // variable initialization

    int y = x/2;  //expression - anything that can be evaluated (to 1 value)
    printf("%i", y);
    return 0;
}