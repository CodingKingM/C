#include <stdio.h>

int main()
{

    int scores[3];

printf("score:");
scores[0];
scanf("%i", &scores[0]);
printf("score:");
scores[1];
scanf("%i", &scores[1]);
printf("score:");
scores[2];
scanf("%i", &scores[2]);
    

    printf("Average %f", (scores[0] + scores[1] + scores[2]) / 3.0);
    return 0;
}